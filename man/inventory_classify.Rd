% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inventory_classify.R
\name{inventory_classify}
\alias{inventory_classify}
\title{Classify files in an inventory table}
\usage{
inventory_classify(
  dt,
  path_col = "path",
  name_col = "name",
  size_col = "size",
  isdir_col = "isdir",
  mount_col = "mount"
)
}
\arguments{
\item{dt}{A \code{data.frame} or \code{data.table} containing file inventory records.
The table must contain a path column. File name, size, directory flag, and
mount/source columns are optional but recommended.}

\item{path_col}{Character scalar. Name of the column containing the file path
within the scanned source. Defaults to \code{"path"}.}

\item{name_col}{Character scalar. Name of the column containing the file or
directory name. Defaults to \code{"name"}. If missing from \code{dt}, it is derived
from \code{basename(path)}.}

\item{size_col}{Character scalar. Name of the column containing file size in
bytes. Defaults to \code{"size"}. If missing from \code{dt}, \code{size} is set to
\code{NA_real_}.}

\item{isdir_col}{Character scalar. Name of the logical column indicating
whether a record is a directory. Defaults to \code{"isdir"}. If missing from
\code{dt}, \code{isdir} is set to \code{FALSE}.}

\item{mount_col}{Character scalar. Name of the column identifying the source,
mount, drive, remote, or storage location. Defaults to \code{"mount"}. If present,
it is used to build \code{location_path}; if absent, \code{location_path} is set equal
to \code{path}.}
}
\value{
A \code{data.table} containing the original inventory records plus GIS
classification columns, including:

\describe{
\item{\code{location_path}}{Mount-aware path used internally for grouping and
classification.}
\item{\code{file_name}}{Normalised file name derived from the input name
column.}
\item{\code{data_file_rel_path_full}}{Original path within the scanned source.}
\item{\code{file_path}}{Path used for classification, usually equal to
\code{location_path}.}
\item{\code{data_file_extension}}{Normalised file extension, including compound
GIS sidecar extensions such as \code{tif.aux.xml} and \code{shp.xml}.}
\item{\code{file_stem}}{Cleaned logical file stem used for sidecar grouping.}
\item{\code{file_dir}}{Directory component of \code{location_path}.}
\item{\code{file_key}}{Grouping key based on directory and cleaned file stem.}
\item{\code{dataset_root}}{Logical dataset root for recognised compound
datasets, such as \code{.gdb} folders or ArcInfo Grid directories.}
\item{\code{container_type}}{Detected container type, where applicable.}
\item{\code{is_container}}{Logical flag indicating whether the row belongs to a
recognised compound GIS dataset container.}
\item{\code{data_file_format}}{Classified file or dataset format, for example
\code{raster}, \code{vector}, \code{esri_file_geodatabase}, \code{esri_arcinfo_grid},
\code{gis_sidecar}, or \code{other}.}
\item{\code{file_role}}{Role of the record, such as \code{file}, \code{sidecar},
\code{dataset_container_component}, or \code{other}.}
\item{\code{primary_dataset}}{Logical flag indicating whether the row represents
a primary dataset candidate.}
\item{\code{group_family}, \code{group_key}, \code{sidecar_count}, \code{sidecar_exts},
\code{sidecar_summary}}{Sidecar grouping and summary fields.}
}
}
\description{
Adds dataset classification fields to an existing inventory table.
The input may come from a local filesystem scan, \verb{rclone lsjson}, a NAS scan,
or any other inventory process that produces file-level records.
}
\details{
This function does not perform the filesystem scan itself. Instead, it
classifies already-scanned records into logical file roles and formats,
including primary datasets, sidecar files, and components of compound GIS
datasets such as Esri File Geodatabases and ArcInfo Binary Grids.

If a mount/source column is present, the function constructs a mount-aware
\code{location_path} using \code{file.path(mount, path)}. This prevents files from
different mounts or storage sources from being incorrectly grouped together
when they share the same relative path, file name, or file stem. The original
\code{path} column is preserved as the path relative to the source.

The function is designed for cached or precomputed inventories. It is useful
when the scan step is handled elsewhere, for example by \verb{rclone lsjson}, GNU
\code{find}, or another storage inventory process.

For single-folder scans, \code{path} alone is usually sufficient for grouping.
For multi-source scans, such as inventories combining Teams drives, OneDrive
remotes, NAS paths, and local SMB mounts, a mount/source column should be
supplied. In that case, sidecar grouping and compound dataset detection are
based on \code{location_path} rather than raw \code{path}.
}
\examples{
inv <- data.table::data.table(
  mount = c("gis_smb", "gis_smb", "teams_xdrive"),
  path = c(
    "landcover/salc1314wdd/hdr.adf",
    "landcover/salc1314wdd/vat.adf",
    "roads/roads.shp"
  ),
  name = c("hdr.adf", "vat.adf", "roads.shp"),
  size = c(1000, 2000, 5000),
  isdir = FALSE
)

classified <- inventory_classify(inv)

classified[, .(
  mount,
  path,
  location_path,
  dataset_root,
  data_file_format,
  file_role
)]

}
